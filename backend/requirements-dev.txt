# Development & test dependencies (NOT required to run the app in production).
#
# Install with:  pip install -r requirements-dev.txt
# This pulls in everything from requirements.txt, then layers dev-only tools.
# Pinned (incl. transitive) to match the project's reproducible-build convention.

-r requirements.txt

iniconfig==2.1.0
packaging==26.2
pluggy==1.6.0
pytest==8.4.2
tomli==2.4.1

# Needed by fastapi.testclient (TestClient) for the API tests.
certifi==2026.6.17
httpcore==1.0.9
httpx==0.28.1
sniffio==1.3.1
